'use strict';

var React = require('react');

var App = React.createClass({
    render: function () {
        return (
            <div>NodeJS.React.Flux</div>
        );
    }
});

module.exports = App;